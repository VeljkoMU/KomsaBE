
class NewsItem {
    constructor(title, image, content, source, url, dateTime) {
    this.title = title,
    this.image= image,
    this.content= content,
    this.source= source,
    this.url= url,
    this.dateTime= dateTime
};
}

module.exports = NewsItem;